# 배포 시 basePath 를 덮어쓰고 싶을 때만 설정합니다.
# (기본값: 개발=빈 문자열, 프로덕션 빌드=/portfolio — next.config.mjs 참고)
# 예) 루트 도메인이나 다른 레포명으로 배포할 경우:
# NEXT_PUBLIC_BASE_PATH=/my-portfolio

# PDF 생성 시 대상으로 삼을 서버 주소 (기본: http://localhost:3000)
# PDF_BASE_URL=http://localhost:3000
